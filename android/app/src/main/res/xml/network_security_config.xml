<?xml version="1.0" encoding="utf-8"?>
<!-- Network Security Configuration for Play Protect Compliance -->
<network-security-config>
    <!-- 
        Production configuration - only allow HTTPS connections
        This prevents man-in-the-middle attacks and ensures data security
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- 
        Debug configuration - allows localhost connections for development
        This will be stripped out in release builds
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- 
        Domain-specific configurations for Firebase and other services
        Ensures proper certificate pinning for enhanced security
    -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebase.googleapis.com</domain>
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">firebasestorage.googleapis.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">gstatic.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
